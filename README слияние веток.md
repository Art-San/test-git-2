git branch feature/main-page  добавляем ветку

git push origin feature/main-page

git merge feature/main-page  перенос ветки но сначала надо переключится на ту в которую будем прносить

git push origin master